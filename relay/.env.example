# constella — Relay Environment Configuration
# Copy to .dev.vars for local dev with `npx wrangler dev`.

# Soroban RPC endpoint
RPC_URL=https://soroban-testnet.stellar.org

# Comma-separated list of contract IDs to poll for events
CONTRACT_IDS=

# Poll interval in milliseconds
POLL_INTERVAL_MS=30000
